# 更新日志 (Changelog)

## 1.6 (2026-09-10)

### 🐛 修复：皮肤偶尔贴到「假候选框」旁边

- **问题**：一些**根本不该算候选框**的弹层被误判成候选框，图片就跟着贴过去 ——
  比如 Electron/Edge 的 Alt 菜单、浏览器的网址提示与收藏夹面板、
  以及切换大小写时 Rime 自己弹的 32×32 状态小窗。
- **修复**：候选框判定统一成一套规则（必须 `ATL:` 类名 + 排除 weasel 进程内的微缩小窗），
  上述误贴场景全部消失，正常跟随不受影响（实机四场景复测正常，回归 13/13）。

### 新增：动图支持（GIF / 动图 WebP / APNG）

> 灵感档案升级一（方案 A+B）+ 向导「预览动画」按钮，本次一次做完。

- **帧序列播放**：`n_frames > 1` 判定动图；每帧走既有品红管线（缩放 → 动态键色 →
  抠色），主线程 `after(帧时长)` 换帧。**静态图路径不动**，行为与 1.5 完全一致。
- **帧时长**：读源文件每帧 `info['duration']`（缺省 100ms），钳到 20~1000ms，
  防异常 duration 把 UI 拖死。
- **抠色键**：多帧颜色并集（≤48 帧全取，更多帧均匀采样且含首末帧）——
  任一动图帧含品红也会自动换键，不会被抠穿。
- **内存 / CPU 保护（方案 B）**：
  - 帧**按需解码** + LRU 6 张 PhotoImage 上限（当前显示帧永不淘汰）；
  - 顺序播放时预解下一帧，消 seek 卡顿；
  - **窗口隐藏即暂停播放**（每 200ms 仅探活一次）—— 最大 CPU 省钱点；
  - 每帧按 base_height 缩放后才做 PhotoImage，不整帧留存原尺寸（天花板 1000×1000 同效）。
- **向导**：选中动图后按钮变「▶ 预览动画(N帧)」，按需播放；不点只显示静态首帧（不卡 UI）。
- 自测：新增 `B_test_anim_sim.py`，**16 项全过**（帧采样 / 时长钳位 / 多帧换键 /
  按需解码+LRU 上限 / 静态图回归）。

### 新增：开机自启开关

- 向导新增「⑩ 开机自启」复选框（勾选态直接读启动文件夹实况）+ **托盘菜单同名开关**（带勾选态）。
- 复用原有 `install/uninstall_autostart`（启动文件夹 `.lnk`，WindowStyle=7 最小化），
  新增 `autostart_installed()` / `set_autostart()`：幂等、失败给提示、只动快捷方式。
- 源码模式也能用（同目录 `pythonw.exe` + 脚本路径），不再只认 exe；CLI `--install/--uninstall` 保留。

### 优化：换配置/重启时体面关旧实例（S8 审查遗留）

- `_kill_existing` 从「powershell CIM + Stop-Process 强杀（实测 5-15s）」改为纯 ctypes 三步：
  ① 枚举同名进程的顶层窗口（GetWindowThreadProcessId + QueryFullProcessImageNameW，
  不再 spawn powershell）；② `PostMessage(WM_CLOSE)` 请它自己退；③ 超时才
  `TerminateProcess` 强杀（也只对「确认还活着」的 pid，用 GetExitCodeProcess 判活）。
- **实机踩到的坑（已修）**：`overrideredirect` 窗如果不注册 `WM_DELETE_WINDOW` 协议，
  WM_CLOSE 只会在系统层面干掉窗口、Tk 记账不对 → 进程残留在后台（假死）。
  所以主窗新增 `root.protocol('WM_DELETE_WINDOW', _graceful_quit)`；
  `_graceful_quit` = 停动画节拍 + 收托盘 + destroy（右键菜单 / Ctrl+Alt+Q / 托盘退出 共用）。
- 实测（模拟实例）：新版实例收到 WM_CLOSE 后 **0.06s 干净退出**（退出码 0，未走强杀）；
  故意不响应的顽固实例 **2.15s 后强杀兜底**（退出码 1）。语义不变：旧实例一定被换掉。
- 另：root 销毁时取消动画 `after`，消除退出瞬间的 `invalid command name ..._anim_tick` 噪声。

### 新增：显示期特效（圆角 / 整体模糊 / 点阵羽化）

- 向导新增「⑨ 特效」（圆角 0~120px、整体模糊 0~30px）与「⑩ 点阵羽化」（带宽 0~80px），预览实时生效。- **只影响显示，不改图片文件**：参数存在 config / 皮肤档案里，随时开关可逆。
- 三个特效各管一块（回答「会不会和抠图的透明通道打架」）：
  - **整体模糊**：作用在 RGB 上（整张图柔化），不受 alpha 二值化影响；
  - **圆角**：作用在 alpha 上；`-transparentcolor` 只支持硬透明，所以按 **4× 超采样**
    绘制+缩回，让硬边尽量贴合轮廓；与抠图共存时是 alpha 相乘，不会把已透明的地方弄回来；
  - **点阵羽化**：单色键抠透明做不出真渐变，所以用 **4×4 有序抖动**把羽化带内的 alpha
    近似成「点阵半透明」——远看像边缘渐隐，贴近看是细点阵；真羽化要等 2.0 分层窗。
- 动图逐帧同样套特效（静态/动图共用同一条管线）。
- **2026-09-10 晚调整（用户反馈）**：取消「边缘羽化」（只柔化边界带的那个方案），
  **只保留「点阵羽化」**作为唯一羽化实现；旧配置的 `feather_dither` 字段仍兼容读取，保存时清理。

### 界面调整：水平翻转上提 + 整体收紧（2026-09-10 晚）

- **水平翻转从「图片预处理」搬到主界面**（③ 贴边方向那一行）：改为**显示期开关**——
  不动图片文件、随时可逆，**动图也一并生效**；预处理对话框只剩裁剪 / 抠图。
  旧配置无需迁移（新增 `flip_h` 字段，默认关）。
- **配置窗口再收紧**：① 缩放 / 水平 / 垂直三个滑块合并为一行，顶部提示压成一行，
  预览画布 620×300 → **620×260**，行间距整体调小。
  窗口尺寸：1016×794 → 876×714 → **992×637**（高度再降 ~80px，宽度略增以换取竖向空间）。

### 界面：配置向导改两栏布局（2026-09-10）

- 原来所有设置竖着一长条，窗口太高。现在：
  - **左栏（普通设置）**：① 图片、② 候选框类型、③ 贴边方向、④ 缩放、⑤ 水平微调、⑥ 垂直微调、⑦ 预览；
  - **右栏（⚙ 高级设置）**：⑧ 图层、⑨ 特效、⑩ 点阵羽化、⑪ 皮肤管理、⑫ 开机自启；
  - 底部：保存并启动 / 取消 / 清理垃圾。
- 窗口高度明显变矮（内容改横向展开），预览区大小不变。

### 新增：清理垃圾

- 向导「🧹 清理垃圾…」+ 托盘同名菜单：清掉程序目录下的临时/备份文件
  （`cfg_image_*` / `preprocessed_*` / `*.bak-*` / 非 error.log 的日志 / `*.tmp` 等）。
- **安全设计**：只删「本目录下的普通文件」——子目录（skins/、build/…）一律不碰；
  白名单（config.json、error.log、README、CHANGELOG、LICENSE、icon.*）不碰；
  受保护类型（.py/.spec/.ico/.md/.json/.exe/.dll…）与点文件（.gitignore）不碰；
  **config.json / 皮肤正在引用的图片不碰**；删除走**回收站**（FOF_ALLOWUNDO），可还原。
- 弹确认框列出将要清理的文件与体积，确认后才动手。
- **冗余副本也算垃圾**（实测反馈）：`config.json` 指向的 `cfg_image_*` / `preprocessed_*`
  副本，若皮肤档案（skins/*/image.png）里存有**字节完全相同**的同一张图，
  则视为重复副本 → 清理时**先把 config 指向改到皮肤那份，再删副本**，不影响使用。

### 优化：error.log 改成「用来反馈问题」的日志

- **用法**：遇到问题**把程序目录里的 `error.log` 发出来即可** —— 它按时间戳把环境、关键动作、异常都记好了。
- 启动时记**环境指纹**：版本、是否打包、Python、系统版本、**屏幕 DPI**、启动参数；
  运行中只记关键点（配置保存 / 切皮肤 / 预处理 / 自启开关 / 清理 /
  **候选框命中（类名+尺寸+进程）** / 异常堆栈），**日常打字与跟随不记** → 长期用也不会膨胀。
- **超 256KB 自动轮转**成 `error.log.1`（只留一份）。

### 修复：用户实测反馈的三个问题（2026-09-10）

1. **托盘图标连点会叠出多个配置窗** —— 每点一次都会排一个「开配置向导」回调，
   向导是嵌套 mainloop，关掉一个又弹下一个。
   修：`open_wizard` 加单实例守卫（已开着就只把窗口提到前面）+ 进程内单外挂实例守卫
   （新实例启动时先关掉上一个，避免叠出多托盘图标/多窗口）。
2. **GIF 预处理后变成皮肤就不动了** —— 预处理一律存单帧 PNG，动图信息丢失。
   修：预处理全程动图感知——同一套裁剪/镜像/抠图逐帧应用，输出 **APNG**
   （叠帧 + 每帧时长），皮肤继续会动；帧数 >300 时均匀采样；逐帧限幅 1200px 控体积；
   向导在预处理后重新点亮「▶ 预览动画(N帧)」。
3. **开机自启会直接把窗口怼出来** —— 启动路径会弹「直接启动 / 重新配置」选择框。
   修：自启快捷方式带 `--tray`，`main()` 识别后**静默启动到托盘**（不弹框、不显窗口；
   已有实例/尚无配置都静默退出）；勾选自启时强制重写快捷方式（旧快捷方式自动升级）。

---

今天顺手把 OpenClaw 更新了，也终于找到了 **DS 用着太费钱**的原因：
因为我把「新版本的功能」在老版本上搓出来了绝大部分……本来就穷，还这么费钱，
**气愤**。明天一定努力摸鱼 (ง •̀_•́)ง

---

## 1.5 (2026-09-04)

> ✨ 感谢热心网友的思路，让我坚定了做挂件而不注入的路子——不改输入法本体、不注入进程，纯外挂透明小窗，干净又安全 (◍•ᴗ•◍)✧

### ⚡ 优化
- **引擎换代**：50ms 全桌面窗口轮询 → WinEventHook 事件驱动 + 候选框句柄缓存，不再每拍全桌扫描（本次重构由 DeepSeek Harness 驱动完成）
- **跟随延迟**：100-200ms → **~20ms**；CPU 占用降一个数量级（实测 25-38ms/s → ~2ms/s）
- **窗口移动直调** SetWindowPos，置顶改为按需触发，不再每拍重绘/强制置顶
- **去抖**：候选框位置微抖 <2px 不移动；闪断 150ms 内不隐藏，减少无谓重绘与闪烁

### ✨ 新增
- **图层选项**：图片可置于候选框【上方】盖住 / 【下方】被其压住（下方仅 居中重叠 时生效，需候选框为置顶窗口）；配置向导预览实时直观表现叠放顺序，所见即所得
- 配置向导新增「图层」单选，带适用条件提示

### 🔧 修复 / 改善
- 候选框销毁重建（切换应用/切换输入法）后自动恢复跟随，不再跟丢目标
- 定位热路径异常写日志，不再静默吞错
- 图层配置随切皮肤/热重载正确同步

### ⚠️ 已知限制
- 「下方」图层仅在 贴边=中间（图片与候选框重叠）时可见；候选框非置顶窗口时自动保持置顶

---

摸鱼更新真爽，也给 deepseek harness 吃 token 爽了，梁乞钞啊这是 (´▽ʃ♡ƪ)

---

## 1.3 (2026-09-01)

### ✨ 新增
- **多皮肤管理**：皮肤 = 图片 + 全套参数，存 `skins/<名称>/` 独立档案；托盘「皮肤选择」子菜单 + 向导皮肤分区，**选中即应用**（含正在运行的外挂，托盘选中态实时同步）
- **动态抠色键**：统计图片颜色选一个图中不存在的颜色当透明键，**图片含品红不再被抠穿**；向导带冲突提示
- **皮肤管理独立分区**：移出图片行，LabelFrame 分组，界面更清爽

### 🔧 修复
- **多 Tcl 解释器分裂（系统性）**：PhotoImage 与 tk 变量（StringVar 等）全部显式传 master——修复「image 'pyimageN' doesn't exist」「删除皮肤/滑块/单选失效」「托盘重配后预览报错」
- **图片窗偶发被候选框压住**：每 50ms 保持置顶（HWND_TOPMOST，不重绘无闪烁）
- **预处理产物持久化**：唯一文件名 + `cfg_image_<ts>` 副本，重新预处理不再串图
- **热重载异常兜底**：图片被删/损坏不崩主线程，记日志继续轮询
- **大图统计性能**：面积阈值 + NEAREST 缩小（1000×1000 图从 3s/118MB 降到 <1s）
- **滚轮缩放失效**：PIL PhotoImage 无 zoom 方法，改走 scale 统一管线
- **托盘切皮肤持久化**：重启仍生效；菜单选中态线程安全；手动显示立即定位

### 🗑️ 移除
- 图层选项（候选框上/下方）、皮肤「应用」按钮（选中即应用）

---

摸鱼被抓，后续更新会慢(´；ω；`)
