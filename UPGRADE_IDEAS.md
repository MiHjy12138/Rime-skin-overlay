# Rime 皮肤外挂 · 升级灵感存档

> 2026-09-01 记录（公子拍板）。**下次升级本工具时，先读这份文档再动手。**
> 状态：✅ 升级二（动态颜色键）+ 升级三（多皮肤管理）已实现（v1.3，2026-09-01）；🚧 升级一（动图）待实现

---

## 背景：当前架构（v1.2）

```
Image.open → RGBA → 等比缩放(base_height×scale) → 品红合成抠色(_flatten_alpha_for_tk) → PhotoImage → Label
```

- 窗口：tkinter 置顶无边框，`-transparentcolor '#FF00FF'` 精确匹配品红抠色
- 跟随：50ms `EnumWindows` 轮询小狼毫候选框（TSF 窗口），自动显隐
- 热重载：2s 查图片 mtime，变了重载
- 已知限制（源码注释）：**图内不能有纯品红像素**，否则被抠穿

---

## 升级一：动图支持（GIF / 动图 WebP / APNG）

**方案 A（主体）**：Pillow `ImageSequence.Iterator` 读帧，`n_frames > 1` 判定动图
- 每帧走现有品红管线 → 预生成 PhotoImage 帧列表 → `after(帧间隔)` 循环换帧
- 滚轮缩放：对帧列表批量 zoom/subsample，逻辑不变
- `_photo_refs` 防 GC 机制已有；mtime 热重载天然支持换动图
- 改动量：~150 行

**方案 B（保护措施，与 A 一起做）**：
- 帧 > 100：`img.seek(i)` 按需解码当前帧 + 预解下两帧（防 500×800 RGBA 200 帧 = 320MB 内存爆炸）
- **窗口隐藏时暂停播放**（最大的 CPU 省钱点）
- 帧 > 1000×1000：先降采样再播
- 向导加「▶ 预览动画」按钮，按需播放不卡 UI

**预览策略（公子确认方向）**：向导预览显示**首帧静态图**；overlay 运行时才逐帧解码+抠色

**性能账**：composite 2-5ms + PhotoImage 转换 10-20ms/帧（800×800）→ 15-30fps 稳；GIF 通常 8-20fps 足够

**坑**：
- GIF 二值透明 → 边缘锯齿，引导用户用动图 WebP / APNG（真 alpha）
- 动图某帧含键色像素会被抠穿 → 见升级二
- Tk 8.6 原生 `PhotoImage.start()` 能播 GIF，但绕过统一品红管线、不支持 APNG/WebP、缩放联动玄学 → **别走**

---

## 升级二：品红管线进化 → 动态颜色键（✅ 公子已拍板方案 1）

**问题**：固定 `#FF00FF` 键色，用户图片含品红像素即被误抠。

**方案**：
1. 加载图片时统计颜色分布（PIL `getcolors` / 采样），**动图取所有帧颜色并集**
2. 选一个图中**完全不存在的颜色**当键色：品红优先 → 冲突则偏移扫描（256³ 空间几乎总能找到）
3. `-transparentcolor` 跟随键色；`_flatten_alpha_for_tk` 用动态键色合成
4. 向导加冲突检测提示：「检测到图片含品红像素，已自动切换抠色键」
5. 改动集中在 `_flatten_alpha_for_tk` + 窗口初始化，几十行；静态图/动图通吃

**远期 v2 拒绝项**：真 alpha layered window（ctypes `UpdateLayeredWindow` / GDI+）——无键色、可半透明阴影，但 tkinter 渲染层要重写（Label 自绘 + 事件/菜单/托盘适配），现阶段性价比不值。**等公子想要「柔和阴影 + 半透明立绘」再启动。**

---

## 升级三：多皮肤管理（2026-09-01 追加，公子拍板）

**需求**：设置图片皮肤后，每个皮肤文件单独保留（不覆盖），需要时随时切换；
- **托盘菜单增加「皮肤选择」子菜单**：列出所有已保存皮肤，点选即切换
- **主配置窗口提供「选择皮肤」按钮 + 「删除某个皮肤」功能**

**要点**：
- 皮肤 = 图片 + 配套参数（布局/贴边/缩放/偏移）整体保存为独立档案，切换时整套恢复
- 存储建议：exe 同目录 `skins/` 文件夹，每皮肤一个子目录（图片 + 参数 json）；或统一一个 skins.json 索引 + 图片文件
- 切换后热重载逻辑复用现有 mtime 检测（或直接触发 `load_char`）
- 删除皮肤：仅删档案，不影响当前正在使用的（若删的是当前皮肤，提示确认后回退默认）
- 托盘菜单已有先例（隐藏/退出），子菜单用 `tk.Menu.add_cascade` 即可

---

## 审查遗留（2026-09-01 MiniMax 审查，v1.3 不采纳/远期）

- **S7 候选框识别加标题验证**：`GetWindowTextW` 验「Weasel/小狼毫」字样做第三保险——现有 TSF 样式三件套 + 进程兜底已稳定，过度工程，等小狼毫改版失灵再说
- **S8 `_kill_existing` 改用 WM_CLOSE**：现在 powershell CIM 杀旧实例 5-15s（历史问题），换成对 mutex 进程发 WM_CLOSE 更快更稳——下次动工时顺手做

---

## 图层选项复盘（2026-09-01 v1.3 回归，MiniMax 追加审查）

v1.2 中期「图层（候选框上/下方）」因 z-order 不稳定被移除（6b6333e），v1.3 修复后回归：

- **根因 ①**（3ffa5f5 引入）：above 分支不做事，below 的 `SetWindowPos(插非topmost候选框后)` 清除图片窗 `WS_EX_TOPMOST`，切回 above 永久失效
- **根因 ②**（实机诊断发现，隐藏）：tkinter `winfo_id()` 返回**内层子窗口**，`-topmost`/`-transparentcolor` 在**顶层窗口**（`GetAncestor(GA_ROOT)`）；v1.2 用子窗口句柄 SetWindowPos 操作错了窗口
- **修复**：每 50ms poll 显式应用；绝不用 HWND_TOP(0)；above=HWND_TOPMOST(-1) 防漂移，below=HWND_NOTOPMOST(-2)+插候选框后；全部操作顶层句柄；实机验证切换无漂移
- 教训：**Tk 窗口句柄 ≠ winfo_id**，Win32 窗口操作一律先 GetAncestor(GA_ROOT)

---

## 升级清单（下次动工时按此执行）

- [x] 动态颜色键：并集选键 + transparentcolor 跟随（方案 1）✅ v1.3 已实现（2026-09-01）
- [x] 向导：品红冲突提示 ✅ v1.3 已实现
- [x] 多皮肤管理：托盘「皮肤选择」子菜单 + 主配置窗选择/删除按钮（升级三）✅ v1.3 已实现
- [ ] 动图支持：帧序列 + after 驱动（方案 A）
- [ ] 保护措施：按需解码 / 隐藏暂停 / 尺寸上限（方案 B）
- [ ] 向导：预览动画按钮
- [ ] 回归：静态图行为不变、热重载、缩放、托盘、跟随逻辑（v1.3 已做冒烟，动图版再做）
